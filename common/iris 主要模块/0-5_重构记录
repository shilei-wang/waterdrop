
1）查询随机数的命令设计比较奇怪
原先的设计：
iriscli rand query-rand  *****
重构后的设计：（rand重复了2次容易混淆）
iriscli query rand rand ****
htlc 也有这个问题
iriscli query htlc htlc c29a9fee2e7848efd14430b8044739cb46a8937c7871915277e2d7e7834814ed --chain-id=shilei-qa

2） iriscli query rand query-queue  有问题，返回不正确

3）example里面的fee显示 **iris，实际不能使用iris （iriscli tx guardian add-profiler -h）

4） htlc refund之后无法查询，查询逻辑错误
=====================================================================


测试 v1.0.0 先期发现的一些需要注意的问题：

1） iriscli不会创建$HOME/.iriscli ，但是使用时默认的home却是$HOME/.iriscli
2） iriscli config 不可用，提示 config.toml does not exist
3） iriscli version 返回错误
4） keys export 不支持直接导出到文件
5） 在tx send， 如果fee含有小数，则会引起panic
6） tx send中 --from描述不准确，实际不可用
7） tx 中generate-only 不可用
8） 节点下线后jail的功能不正常，不会被jail
9） swagger暂时不可用， 前缀https://stargate.cosmos.network/需要改掉

api差异

总体不同：
1） iris 有type value的格式，cosmos没有
2） result 里面不同格式不同 iris有   "Data"，"Log"
3） iris 和 cosmos广播的格式不同（iris是 POST/tx/broadcast ， cosmos是）
4） iris的post里用的是base_tx， cosmos用的是base_req，里面的格式不尽相同

具体不同：
GET /txs
iris 用的是tag的方式和cosmos不同

GET /node_info
格式不同：
iris是protocol_version， cosmos是protocol_version+node_info

GET /validatorsets/latest
有些参数的key值不同，比如iris是power，accum；cosmos是voting_power，proposer_priority

GET /bank/balances/{address}
iris包含余额和account信息， cosmos则是分开余额和account信息分别查询

POST /bank/accounts/{address}/transfers
send交易
iris和cosmos的发请求的地址不同，post内的格式也有较大的不同。

GET /staking/delegators/{delegatorAddr}/delegations
cosmos比iris 多了balance字段，少了height字段

POST /staking/delegators/{delegatorAddr}/delegations
cosmos比iris多了delegator_address

GET /staking/redelegations
请求的地址格式不一样， iris是GET /stake/delegators/{delegatorAddr}/redelegations

POST /stake/delegators/{delegatorAddr}/redelegations
iris要比cosmos多了share percent功能

GET /staking/delegators/{delegatorAddr}/validators/{validatorAddr}
cosmos比iris 多了 "bond_intra_tx_counter"

GET /stake/validators
cosmos比iris 多了 "details"和"bond_intra_tx_counter"

GET /staking/pool
两者返回内容有较大不同， cosmos 有inflation 相关信息

GET /staking/parameters
cosmos比iris多了  "inflation"，"goal_bonded" 字段

GET /gov/proposals
iris比cosmos 多了查询latest proposals的功能

POST /gov/proposals/param_change
cosmos用的是独立的param change， iris是把所有提议都放在submit proposal里面

GET /gov/proposals/{proposalId}/proposer
这个根据id查proposer的接口 iris没有

GET /gov/proposals/{proposalId}/tally
iris没有这个接口， iris把tally结果统一放在proposal中

GET /gov/parameters/deposit
GET /gov/parameters/tallying
GET /gov/parameters/voting
这三个接口iris都没有，  iris都是在cli中用iriscli params查询

GET /slashing/signing_infos
这个接口与iris不一样， iris是 GET /slashing/validators/{validatorPubKey}/signing-info

POST /slashing/validators/{validatorAddr}/unjail
这个接口的post内容和iris完全不一样，包含signature字段

GET /slashing/parameters
iris没有这个接口

Distribution
cosmos的Distribution接口和iris有较大不同， iris是吧3种withdraw合并在一个接口中， cosmos是分开的

GET /distribution/community_pool
GET /distribution/parameters
iris没有这两个接口

GET /supply/total
GET /supply/total/{denomination}
iris没有这两个接口

GET /minting/parameters
GET /minting/inflation
GET /minting/annual-provisions
iris没有这3个接口
